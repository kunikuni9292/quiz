name: my_mario_game
description: "Flame を使ったマリオ風プラットフォーマー（まずは Container でダミー表示）"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.17.0
  cupertino_icons: ^1.0.2
  window_size: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/img/    # いったん中身は空でも可
    - assets/images/ 